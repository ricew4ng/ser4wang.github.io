# set `baseurl` to your root domain
# if you set it to "/" share icons won't work properly on production
baseurl = "https://sera.wang/"  # Include trailing slash
title = "Rice"  # Edit directly from config/_default/languages.toml # alternatively, uncomment this and remove `title` entry from the aforemention file.
copyright = ""
# canonifyurls = true
paginate = 10
theme = "hugo-clarity"
disqusShortname = "serawang"

DefaultContentLanguage = "en"

[params]
# mainSections = ["post"]
body_classes = "avenir bg-near-white"

# [languages]
# config/_default/languages.toml

# [menus]
# config/_default/menus/menu.xx.toml

[taxonomies]
# category = "categories"
tag = "tags"
# series = "series"

[outputs]
home = ["HTML", "RSS", "JSON"]
